<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Área privada (Pacientes Urgencias) - loging</title>
    <!-- LogoApp -->
        <link rel="icon" href="{{ asset('designLogin/Images/Logo-SCS.png') }}"/>

    <!-- Fav-Icons -->
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css"
            integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg=="
            crossorigin="anonymous"
            referrerpolicy="no-referrer"
        />

    <!-- CSS -->
        <link rel="stylesheet" href="{{ asset('css/style.css') }}"/>
        <link rel="stylesheet" href="{{ asset('designLogin/CSS/style.css') }}"/>
        <link rel="stylesheet" href="{{ asset('designLogin/CSS/style.css') }}"/>
        <link rel="stylesheet" href="{{ asset('designLogin/Images/style.css') }}"/>
        <link rel="stylesheet" href="{{ asset('designLogin/CSS/animations.css') }}"/>
        <link rel="stylesheet" href="{{ asset('designLogin/CSS/global.css') }}"/>
        <link rel="stylesheet" href="{{ asset('designLogin/CSS/media-queries.css') }}"/>
      
        <link rel="stylesheet" href="{{ asset('designLogin/CSS/alerts.css') }}"/>
        
    <!-- JS -->
        <script src="{{ asset('designLogin/JS/script.js') }}" defer></script>    
</head>
<body>
    <header></header>

    <main>
        <div id="card">
            <img id="front" src="{{ asset('designLogin/Images/img_front.png') }}" alt="Área de Salud de Lanzarote - HOSPITAL UNIVERSITARIO DOCTOR JOSÉ MOLINA OROSA">
            
			<div id="privateArea">
                <img id="logo" src="{{ asset('designLogin/Images/Logo-SCS.png') }}" alt="Área Privada">
                
				<div id="textPrivateArea">
                    <p id="area">Área</p>
                    <p id="privada">Privada</p>
                </div>
            </div>

            <form id="loginCheck" action="{{path('login_check')}}" method="post">
                <div id="user">
                    <div class="icon">
                        <i class="fa-solid fa-user" style="color: #000000;"></i>
                    </div>

                    <input type="text" id="formUser" name="_username" value="{{last_username}}" placeholder="Usuario de Dominio" required>
                </div>

                <div id="password">
                    <div class="icon">
                        <i class="fa-solid fa-key" style="color: #000000;"></i>
                    </div>

                    <input type="password" id="formPassword" name="_password" placeholder="Clave de Dominio" required>
                </div>

                <!-- Mensaje de error (no genérico para contraseñas incorrectas). -->

                <div id="button">
                    <button type="submit" id="buttonCheck" name="login" value="Acceder"> Acceder</button>
                </div>

                <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
            </form>

            <h5>Gerencia de Servicios Sanitarios - Área de Salud de Lanzarote</h5>
        </div>
    </main>

    <footer></footer>

    <img id="background" src="{{ asset('designLogin/Images/Fondo.png') }}" alt="#">
</body>
</html>

{% block page %}
    {% if error %}
        <div class="error-message">{{ error.message|raw }}</div>
    {% endif %}
{% endblock %}
